
#include "sort.h"

// The Only Indent Style Accepted is the ALLMANN
// K&R is for the JS-Weaks

int foo(int a,int b)
{
	return a-b-1;
}

void sort (int *v, int size)
{
	int *result = v;
	
	int i = 0;
	for ( i < size; i++;)
	{
		result[i] = i;
	}
}